\documentclass[conference]{IEEEtran}
% IEEEtran discards \thanks in conference mode, logging only a plain \typeout
% ("** WARNING: \thanks is locked out...") that no LaTeX warning scan reports.
% Restore it so the funding acknowledgment can be typeset as the unmarked
% first-page footnote. This only re-lets commands and emits nothing on its own.
\IEEEoverridecommandlockouts%

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

% --- Submission anonymity switch --------------------------------------------
% \anonymoustrue  : blind byline, funding acknowledgment suppressed (current).
% \anonymousfalse : named byline plus the funding acknowledgment.
% Anonymity for the KSE 2026 submission is a deferred owner decision, and the
% byline is already blind, so the default below reproduces today's PDF exactly.
% Flipping the single line is the only edit this file needs; the named branch
% still needs the real byline from the owner. Identity leaks the switch does
% not cover are listed in notes/deanonymization-checklist.md.
\newif\ifanonymous%
\anonymoustrue%

% Lean 4 listings setup (minimal; extend as needed)
\lstdefinelanguage{lean}{
  keywords={theorem,lemma,def,structure,instance,noncomputable,exists,fun,let,match,with,by,import,open,namespace,end},
  sensitive=true,
  comment=[l]{--},
  morecomment=[s]{/-}{-/},
}
\lstset{
  language=lean,
  % render the Unicode used in verbatim-quoted Lean statements under pdflatex
  literate={∃}{{$\exists$}}1 {ν}{{$\nu$}}1 {ℝ}{{$\mathbb{R}$}}1
           {₀}{{$_0$}}1 {𝔊}{{$\mathfrak{G}$}}1,
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries,
  commentstyle=\itshape\color{gray},
  breaklines=true,
  columns=fullflexible,
}

\begin{document}

\title{Formalizing Leray--Hopf in Lean~4 with AI Agents:\\
Harness Design and Reliability Lessons}

\ifanonymous%
  \author{\IEEEauthorblockN{Anonymous Author(s)}}
\else
  % TODO(owner): substitute the real byline and affiliation here. The placeholder
  % below is deliberately conspicuous: flipping the switch without supplying the
  % byline would otherwise emit a blind byline together with the searchable grant
  % number, which is the worst of both. An affiliation email address will fail
  % scripts/redact_check.py, which scans paper/.
  \author{\IEEEauthorblockN{TODO: REAL BYLINE NOT YET SUPPLIED}%
    \thanks{This work was supported by JST-Mirai Program Grant Number
      \mbox{JPMJMI22G1}, Japan.}}
\fi

\maketitle

\begin{abstract}
Formalization projects that run for weeks require safeguards beyond the
proof kernel. The kernel checks a theorem against its stated type, but it
does not choose the intended statement, preserve a public interface during
refactoring, or manage the agents and computing resources used to produce
the proof. We report a Lean~4 and mathlib formalization, with no project
axioms, of Leray--Hopf weak solution existence for the unforced
Navier--Stokes equations on $\mathbb{T}^3$ and $\mathbb{R}^3$. To our
knowledge, no prior machine-checked formalization establishes this
three-dimensional weak existence theorem. During six weeks of development,
a workflow defined by the owner routed GitHub Issues through isolated Git
worktrees and pull requests. The harness routes
mathematical, Lean, interface, and resource checks according to the
artifact being changed. Four incidents show where
these checks detected defects and where they did not. The evidence method
links manuscript claims to redacted session records, repository history,
verification results, cost estimates, and billing records.
\end{abstract}
% Abstract claims map: CLM-001 (result), CLM-006 (prior-work comparison),
% CLM-007, CLM-009 (workflow and harness), CLM-008 (incident account).

\begin{IEEEkeywords}
formal verification, Lean 4, Navier--Stokes equations, Leray--Hopf weak solutions,
AI agents, large language models, proof engineering
\end{IEEEkeywords}

\input{sections/01-introduction}
\input{sections/02-formalization}
\input{sections/03-agent-workflow}
\input{sections/04-incidents}
\input{sections/05-discussion}
\input{sections/06-conclusion}

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
