\documentclass[conference]{IEEEtran}
% IEEEtran discards \thanks in conference mode, logging only a plain \typeout
% ("** WARNING: \thanks is locked out...") that no LaTeX warning scan reports.
% Restore it so the funding acknowledgment can be typeset as the unmarked
% first-page footnote. This only re-lets commands and emits nothing on its own.
\IEEEoverridecommandlockouts%

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{listings}
\usepackage{xcolor}
\usepackage[hidelinks]{hyperref}

% --- Submission anonymity switch --------------------------------------------
% \anonymoustrue  : anonymous draft build.
% \anonymousfalse : named byline plus the funding acknowledgment (submission
% target; the default since issue #114 supplied the owner byline).
\newif\ifanonymous%
\anonymousfalse%

% Lean 4 listings setup (minimal; extend as needed)
\lstdefinelanguage{lean}{
  keywords={theorem,lemma,def,structure,instance,noncomputable,exists,fun,let,match,with,by,import,open,namespace,end},
  sensitive=true,
  comment=[l]{--},
  morecomment=[s]{/-}{-/},
}
\lstset{
  language=lean,
  % render the Unicode used in verbatim-quoted Lean statements under pdflatex
  literate={∃}{{$\exists$}}1 {ν}{{$\nu$}}1 {ℝ}{{$\mathbb{R}$}}1
           {₀}{{$_0$}}1 {𝔊}{{$\mathfrak{G}$}}1,
  basicstyle=\ttfamily\footnotesize,
  keywordstyle=\bfseries,
  commentstyle=\itshape\color{gray},
  breaklines=true,
  columns=fullflexible,
  % fullflexible collapses the space after each literate-replaced glyph, so
  % "ν T" printed as "νT" in the verbatim-quoted statements (issue #107).
  keepspaces=true,
}

% --- Claim tags (CLM-/INC-/EV-, "% CLAIM:" comments) -------------------------
% These tags are provenance pointers scanned by scripts/verify_claim_links.py.
% They are not authorities over the text: if a tagged source and the manuscript
% or the pinned Lean sources disagree, the tag settles nothing — re-derive from
% the primary artifact. Never justify an edit or a review verdict by "the claim
% file says so".

\begin{document}

% Must precede every other citation: IEEEtran.bst reads the control entry
% only from the first \citation record in the .aux file. Prints nothing.
\bstctlcite{bstctl:etal}

\title{Formalizing Leray--Hopf in Lean~4 with AI Agents:\\
Harness Design and Reliability Lessons}

\ifanonymous%
  \author{\IEEEauthorblockN{Anonymous Author(s)}}
\else
  % Byline supplied by the owner (issue #114). No email address by design:
  % an affiliation email would fail scripts/redact_check.py.
  \author{\IEEEauthorblockN{Tomoki Uda}%
    \IEEEauthorblockA{Nanzan University}%
    \thanks{This work was supported by JST-Mirai Program Grant Number
      \mbox{JPMJMI22G1}, Japan.}}
\fi

\maketitle

\begin{abstract}
Formalization projects that run for weeks require safeguards beyond the
proof kernel, which checks a theorem against its stated type but does not
choose the intended statement, preserve a public interface during
refactoring, or manage the agents and computing resources that produce
the proof. We report a Lean~4 and mathlib formalization, with no project
axioms, of the classical existence theorem for Leray--Hopf weak solutions
to the unforced Navier--Stokes equations on $\mathbb{T}^3$ and
$\mathbb{R}^3$; to our knowledge no prior machine-checked formalization
establishes it in three dimensions. During
six weeks of development, a conventional GitHub-based workflow routed
Issues through isolated Git worktrees and pull requests, and the author's
harness selected mathematical, Lean, interface, and resource checks
according to the changed artifact. Four
incidents show where these checks detected defects and where they did not,
and a claim-to-evidence method links the paper's tagged claims to
redacted session records, repository history, verification results, and
billing records.
\end{abstract}
% Abstract claims map: CLM-001 (result), CLM-006 (prior-work comparison),
% CLM-007, CLM-009 (workflow and harness), CLM-008 (incident account).

\begin{IEEEkeywords}
formal verification, Lean 4, Navier--Stokes equations, Leray--Hopf weak solutions,
AI agents, large language models, proof engineering
\end{IEEEkeywords}

\input{sections/01-introduction}
\input{sections/02-formalization}
\input{sections/03-agent-workflow}
\input{sections/04-incidents}
\input{sections/05-discussion}
\input{sections/06-conclusion}

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
