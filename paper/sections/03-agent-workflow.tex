\section{Harness Design for Formalization with Agents}\label{sec:workflow}

\begin{figure*}[t]
% ALT: Three actors above a shared GitHub record. The author holds final
% authority and acts as the hub, exchanging context and summaries with
% ChatGPT and handing an issue contract to the agent harness. An unlabelled
% band between the two ChatGPT arrows narrows toward the author, so material
% returns as a summary. ChatGPT is advisory and reaches GitHub only through
% the official GitHub App connector; the author reaches it without ChatGPT in
% the path; the harness reaches it through git and the gh CLI. GitHub is not
% an actor but the medium that records Issues, PRs, and reviews.
\centering
\includegraphics[width=\textwidth]{figures/author-interface-provenance.pdf}
\caption{Author, ChatGPT, and the agent harness above the shared GitHub
record. The author holds final authority over scope and acceptance, exchanges
context and summaries with ChatGPT, and hands an issue contract to the
harness. The unlabelled band between the two ChatGPT arrows narrows toward the
author: material returns as a summary rather than in full. ChatGPT is advisory
and reaches GitHub only through the official GitHub App connector, while the
author reaches it without ChatGPT in the path. GitHub is not an actor but the
medium that records Issues, PRs, and reviews; implementation itself runs in
local Git worktrees. Figure~\ref{fig:agent-harness} expands the Agent harness
box.}\label{fig:author-interface}
\end{figure*}

\begin{figure*}[t]
% ALT: A synthesized three-layer agent harness reconstructed from controls
% introduced over the Leray-Hopf campaign, expanding the Agent harness of the
% preceding figure. The author defines and
% adjudicates an issue contract. A model-independent orchestrator delegates
% bounded work and selects applicable assurance. Implementation occurs in
% an isolated worktree and undergoes semantic or mechanical review as
% required by the changed artifact. Semantic findings return to both the
% author and issue contract; technical findings return to
% implementation. Runtime controls support orchestration and execution.
\centering
\includegraphics[width=\textwidth]{figures/leray-hopf-agent-harness.pdf}
\caption{Synthesized end-state architecture of the agent harness, expanding the Agent harness box of Figure~\ref{fig:author-interface}. Under the author's authority, an orchestrator routes each GitHub Issue through an implementing Git worktree and a PR. Semantic feedback returns to the author and issue contract, while technical rework returns to the worktree. Semantic and Lean checks are selected as applicable to the changed artifact, one or both. Runtime governance supports execution beneath orchestration. Individual controls entered the project at different stages across the campaign.}\label{fig:agent-harness}
\end{figure*}

% Mapping: CLM-007, CLM-009 ← analysis/workflow-evolution.md,
% session indices 74aab39b 90fa24bb 7e6156bf cd98f32a 7dbee3de 8aed0316.
Figure~\ref{fig:author-interface} places the work in three parts above one
shared record. The author held final authority over scope and acceptance.
ChatGPT supported investigation, synthesis, recommendation, and drafting
without deciding. The agent harness carried out issue-scoped work.
Neither ChatGPT nor the implementing agents held independent authority.
% CLAIM: CLM-012

Figure~\ref{fig:agent-harness} expands the agent harness into the synthesized
end-state architecture. Its individual controls were introduced
incrementally over the course of the campaign, including changes made
in response to the incidents in Section~\ref{sec:incidents}; the
diagram reconstructs this final configuration rather than an
unvarying structure. % CLAIM: CLM-007, CLM-009

The work ran across two local computers and an isolated development
container on a virtual private server (VPS). Many parallel orchestration
sessions used the VPS container. Agents worked in separate Git worktrees
but shared the repository, Lean build cache, and a memory ceiling of
3.42\,GiB. This setting made computing resources part of the harness rather
than background infrastructure. % CLAIM: CLM-007, CLM-009, EV-0247

\subsection{Author authority and the GitHub workflow}

The author retained authority over theorem scope. A GitHub Issue
(hereafter Issue) defined each unit of work and its acceptance conditions.
When a proposition could easily be overstated, a separate statement card
recorded its hypotheses, conclusion, and permitted proof boundary. The
Issue bounded implementation. The statement card exposed the mathematical
contract to review. % CLAIM: CLM-009

The main orchestrator assigned agents and checked their returned artifacts
without editing Lean files itself. The repository skill
\texttt{github-driven-workflow} required every change to begin with an
Issue. An implementing agent received its own branch and Git worktree, then
returned a committed candidate through a GitHub PR. The workflow contract
required agents to submit changes through PRs rather than push directly to
the main branch. A PR could merge only after the
checks relevant to its changed artifacts had passed, an independent
reviewer had left an attributable result, and all review threads were
resolved.

Issue scope, review decisions, and merge authority were recorded in
repository and GitHub artifacts rather than retained only in an agent
session. When Fable~5 resumed orchestration through Google Cloud's Vertex
AI on July~2--3, it continued the existing workflow from those artifacts.
% CLAIM: CLM-003, CLM-007, CLM-009, EV-1681

This workflow also separated the destinations of review findings.
Questions about mathematical meaning returned to the author and statement
card. Lean errors and build failures returned to the implementing
worktree. During the large July refactor, the orchestrator first assigned a
scout that could inspect repository assumptions but not edit them, then assigned separate
agents to implementation, module inspection, and comparison of public
declarations. Inspection roles had no edit authority. % CLAIM: CLM-007, EV-1669

\subsection{Checks selected by artifact}

\emph{Mathematical statement.}
An agent independent of the implementer checked whether the proposition
matched the intended mathematics and whether its hypotheses were
sufficient. For statements near a boundary of validity, the reviewer
looked for concrete counterexamples before proof search continued.
Section~\ref{sec:incidents} contrasts a defect found by this procedure
before merge with one that remained until release preparation.
% CLAIM: CLM-005, CLM-007, EV-0925

\emph{Lean declaration and assumptions.}
The Lean build checked elaboration and type correctness.
\texttt{\#print axioms} recorded the assumptions of designated
declarations, and an import check kept experimental modules outside the
public root import. A full build was recorded for the release commit.
% CLAIM: CLM-009

\emph{Public declarations.}
Refactoring was checked against the preceding inventory of public
declarations. When exact preservation was required, the comparison also
covered statement text. This check was independent of downstream use and
therefore detected an unreferenced theorem omitted during refactoring.
% CLAIM: CLM-007, EV-1669

The GitHub workflow preserved these results with the candidate they
assessed. A review request alone was insufficient. The PR needed the
reviewer's identity and result. This prevented a dispatched but unfinished
review from being mistaken for completed assurance.
% CLAIM: CLM-007, CLM-009, EV-1669

\subsection{Worktree assignment, liveness, and computing resources}

Each Git worktree had one active implementing agent, and shared import
files had one designated writer. An apparent loss of contact did not by
itself reassign the worktree. The orchestrator first checked the agent and
its process state, because starting a replacement in the same worktree
could duplicate both edits and builds. % CLAIM: CLM-007, EV-2076, EV-1669, EV-0247

Heavy Lean builds shared a cache and ran under a global lock. Before
starting one, the harness checked memory available to the whole VPS
container and removed helper processes left by completed jobs. Under
pressure, it also reduced Lean's thread count. These requirements were
placed in standing agent instructions and repeated in dispatch prompts.
They did not depend on an agent recalling an earlier incident.
% CLAIM: CLM-007, CLM-009, EV-0247

\subsection{Evidence and measured use}

Session records, GitHub Issues and PRs, build results, and release
artifacts were retained as separate sources. Claim identifiers connect
paper statements to those sources, while reconstructed intervals remain
distinguishable from primary session evidence. % CLAIM: CLM-009

From 2026-06-10 through the release on 2026-07-20, the collected logs
record at least 15.1M output tokens and 199.1h of active session time, a
coarse estimate obtained by joining adjacent events across gaps of at most
five minutes. Applying published API prices to that recorded use gives
\$6{,}973. Apart from the Vertex AI phase below, the agents ran on
subscription plans rather than metered API billing, so this figure is an
API-price-equivalent estimate of recorded use and not an amount billed to
the project. The token count and the estimate derived from it are lower
bounds because some session records were lost or unavailable.
% CLAIM: CLM-002

The July 2--3 completion phase occurred immediately after Fable became
available again. Fable orchestrated that work through Google Cloud's
Vertex AI, the one phase billed per token rather than covered by a
subscription, with Opus agents used for some delegated tasks. The provider
export records a charge of JPY~54{,}868 for all activity on the machine,
while applying published API prices to the project logs gives
\(\$258.28\). If the provider's aggregate token metric measures the same
token classes as the logs, the project's share of the charge is
JPY~41{,}421--41{,}796. % CLAIM: CLM-003, CLM-007

For incident analysis, six sessions were selected for their
reconstruction value and coded independently of the formal result. They
contain four statement mismatches or weakenings, nineteen recovery
actions, twelve resource failures, and five Git worktree assignment
conflicts. No unresolved mismatch between a runtime success report and
the state on disk was found. These counts describe the selected sessions.
They do not estimate rates for the project or for agent systems in
general. % CLAIM: CLM-004
