# INC-004: PR #120 で main の公開定理 `edge_inf_eq_schwartz_tensor` が dedup リファクタ中に無断削除され，byte-diff 悉皆レビューがマージ前検出

- **Incident ID:** INC-004
- **Date and environment:** 2026-07-10，local-main，uda-lab/leray-hopf，`lean-pde`（issue #111 リファクタリング実装），PR #120
- **Trigger:** issue #111（重複排除）の PR-1 として，2 つの convection-extension ファイル間でほぼ逐語的に重複していたテンソル/edge-gluing 層を，`(X, EdgeSpan, BLTLin, BLT_overlap)` で汎化した共有モジュール `LerayHopf/Analysis/TensorEdgeGluing.lean` へ抽出する大規模 dedup リファクタが行われた．
- **Immediate symptom:** レビュアー `prrev-111-1` が宣言単位の byte-diff 照合を実施したところ，R3 側 `LerayHopf/R3/ConvectionExtension.lean` の公開定理群を薄いラッパーとして再配置する過程で，公開定理 `edge_inf_eq_schwartz_tensor` が無断で削除されていることを検出した．
- **Underlying cause:** 重複コアブロック（C4–C10）を削除し，公開名を新モジュールへの薄い instantiation として再配線する作業の中で，`edge_inf_eq_schwartz_tensor` が再配線対象の一覧から漏れ，他の公開定理（`edgeSlot2/3`，`detDomain`，`antisymmetrizer`，`detExtend`，`convFormL2_def` 等）は正しく再配線されたのに対し，この定理だけが main 上の宣言ごと消失した．
- **Affected theorem, file, or campaign:** `LerayHopf/R3/ConvectionExtension.lean` の公開定理 `edge_inf_eq_schwartz_tensor`．issue #111（重複排除，PR-1，leray-hopf#120）．
- **Why existing checks failed:** `lake build` は当該定理を参照する下流コードが存在しなければ green のまま通過し，定理 1 個の消失を検出しない．in-house の modularity-reviewer（import 構造・循環・thin instantiation の検査）も import surface の健全性を見るのみで，宣言単位の網羅的な存否は検査対象外だった．
- **How the problem was detected:** レビュアー `prrev-111-1` が，動的スポーンした 8 体の deep-verify subagent を用いて，PR-1 を含む issue #111/#113/#114 全 12 PR に対し，main 上の全公開宣言を byte 単位で抽出・照合する悉皆レビューを実施し，`edge_inf_eq_schwartz_tensor` が main には存在するが PR-1 の diff には現れない（=削除されている）ことを検出した．
- **Recovery procedure:** 実装コーダー `glue-111` が，Torus 側の対応定理と同型の薄いラッパーとして `edge_inf_eq_schwartz_tensor` を main の元の statement のまま `LerayHopf/R3/ConvectionExtension.lean` に復元し再コミット（コミット `05f3967`）．レビュアーが再検証を実施し，`convFormL2_extends`／`convFormL2_cont_fixedTest` 以降の全公開定理 statement が main と byte-identical であることを再確認した上で PASS 判定，PR #120 をマージした．
- **Permanent guardrail introduced:** 宣言単位の byte-diff 照合を，大規模リファクタ PR（issue #111/#113/#114，計 12 PR）全件に対する標準のマージ前レビュー手法として継続適用した．オーケストレータ自身も 2 つの capstone 定理（`exists_lerayHopf_torus3`／`exists_lerayHopf_r3` 相当）について `#print axioms` が kernel-only（`propext, Classical.choice, Quot.sound`）のまま崩れていないことを全 12 PR を通じて独立に検証した．
- **Generalisable lesson:** 大規模な dedup／重複排除リファクタは，重複コードの削減という正しい目的の遂行中に，公開 API 定理を静かに取りこぼす risk を内在する．ビルド成功は定理の消失を検出しない（下流参照がなければ green のまま通る）ため，宣言単位（statement レベル）の byte-diff レビューをマージゲートに組み込むことで，ビルドでは捕捉できない欠落を検出できる．
- **Primary evidence:** EV-1669，leray-hopf#120，evidence/repository-snapshots/leray-hopf/issues.json
- **Evidence type:** primary（session `7e6156bf` メイン transcript EV-1669 + GitHub snapshot の PR #120 本文）
- **Confidence of reconstruction:** high（一次ログ: session `7e6156bf` メイン transcript を含む EV-1639〜EV-1669/EV-1678 を全走査．PR #120 本文（GitHub snapshot）に「`edge_inf_eq_schwartz_tensor` 削除→コミット `05f3967` で復元」の記載があり session-index の記録と一致．8体の deep-verify subagent の verdict は末尾まで確認済み，subagent の中間試行過程は teammate-message 要約経由のみ）

## 分類（PLAN.md §5，該当するもの全て）

theorem-weakening-vacuous-discharge-assumption-smuggling / successful-detection-and-recovery

## 重要度評価

| 軸 | 評価 | 根拠 |
|---|---|---|
| 影響範囲 | 高 | 削除されたのは main 上の公開定理（API の一部）であり，見逃されていれば後続の利用者・下流証明・issue #113/#114 の後続 PR に影響しうる状態でマージされるところだった． |
| 検出困難性 | 高 | ビルドは定理が削除されても下流参照がなければ green のまま通過し，import 構造検査の modularity-reviewer も統計を検出しない．12 PR・多数の宣言を横断する悉皆的な宣言単位 byte-diff 照合という高コストな手法が必要だった． |
| 回復費用 | 低 | 検出（00:12:57Z）から復元・再検証・マージ（00:17:23Z）まで約4分半．同一 PR 内で即日解消． |
| 再発可能性 | 中〜高 | 本セッション自体が issue #111/#113/#114 で計 12 件の大規模リファクタ PR を扱っており，このクラスの取りこぼしは dedup／モジュール分割作業全般で構造的に起こりやすい（実際，同一手法を全 12 PR に適用する運用へ直後に一般化されている）． |
| 一般化可能性 | 高 | 「ビルド成功は定理の消失を保証しない」という教訓は，Lean に限らず大規模リファクタを伴うあらゆる形式化・ソフトウェアプロジェクトに一般化できる． |
| 一次資料の完全性 | 高 | オーケストレータのメイン transcript（EV-1669 ほか，2187 行全件走査）に WEAKEN/RECOVERY イベントとして直接記録．PR #120 本文（GitHub snapshot，issues.json）に削除・復元コミットの詳細が明記され整合．8体の deep-verify subagent の最終 verdict は個別確認済み． |

## 経緯（詳細）

2026-07-10，issue #111（重複排除）の PR-1 として，R3 と Torus の 2 つの convection-extension ファイル間でほぼ逐語的に重複していたテンソル/edge-gluing 層（edge slots・bilinears・lifts・retractions・`psiD`/`psiSup` の LinearPMap glue・`gInv`・`antisymmetrizer`・`detExtend`・`convFormL2_*` など 72 名）を，`(X, EdgeSpan, BLTLin, BLT_overlap)` で汎化した新モジュール `LerayHopf/Analysis/TensorEdgeGluing.lean`（679行）へ抽出する作業が実装コーダー `glue-111` によって行われた．`LerayHopf/R3/ConvectionExtension.lean` からは重複コアブロック（C4–C10）が削除され（−554行），公開名（`edgeSlot2/3`，`detDomain`，`antisymmetrizer`，`detExtend`，`convFormL2_def`／`_def_eq`／`_multilinear`／`_antisymm`，`edge_inf_eq_schwartz_tensor`）は新モジュールへの薄い instantiation として再配線される計画だった．Torus 側の `LerayHopf/Torus/ConvectionExtension.lean` にも同様の再配線が行われた（−463行）．

事前の in-house レビューとして，modularity-reviewer が import 構造・循環・thin instantiation の健全性を検査し PASS 判定を出した．しかし，このレビューは import surface の健全性を対象とするものであり，宣言単位での存否までは検査していなかった．

2026-07-10T00:12:57Z，レビュアー `prrev-111-1` が，動的にスポーンした8体の deep-verify subagent を用いて，main 上の全公開宣言と PR-1 の diff とを宣言単位・byte 単位で照合する悉皆レビューを実施した．この過程で，`edge_inf_eq_schwartz_tensor` が main には存在するにもかかわらず PR-1 の diff中で再配線対象の一覧から漏れ，宣言ごと静かに削除されていることが判明した．他の公開定理（`edgeSlot2/3`，`detDomain`，`antisymmetrizer`，`detExtend`，`convFormL2_def` 等）は正しく薄い instantiation として再配線されていたため，この 1 件の欠落は差分の分量に埋もれて見過ごされうる性質のものだった．

00:17:23Z，`glue-111` が Torus 側の対応する定理と同型の薄いラッパーとして，`edge_inf_eq_schwartz_tensor` を main の元の statement のまま `LerayHopf/R3/ConvectionExtension.lean` に復元し，コミット `05f3967` として再コミットした．レビュアーが再検証を行い，`convFormL2_extends`／`convFormL2_cont_fixedTest` 以降の全公開定理 statement が main と byte-identical であることを確認した上で PASS 判定を出し，PR #120 がマージされた．

この宣言単位 byte-diff 照合は，以後 issue #111/#113/#114 の計 12 PR 全件に対する標準のレビュー手法として継続適用され，本件以外に検出されたのは可視性変更 1 件（non-blocking）のみだった．全 12 PR のマージを通じて，2 つの capstone 定理の `#print axioms` は kernel-only（`propext, Classical.choice, Quot.sound`）を一度も崩していないことがオーケストレータ自身によっても独立に確認されている．本件は，ビルド成功では検出できない公開 API の静かな欠落を，宣言単位の悉皆的な byte-diff レビューがマージ前に捕捉した事例である．
