# Session de129390-559a-4ecd-950e-3667cf1c1c3c

- **host**: vps
- **日時範囲（UTC）**: 2026-07-15T09:51:29Z 〜 2026-07-18T15:49:29Z（約3日間，断続稼働．本記録が対象とするのは 2026-07-18 の OOM cascade 区間，UTC 03:59〜13:19）
- **working directory**: `/tmp/hermes-worktrees/lean-pde-notes-issue-51`（`uda-lab/leray-hopf` および `uda-lab/leray-hopf-notes` の並行オーケストレーション，scratchpad 配下に複数 worktree `wt-*`/`nwt-*` を展開）
- **関与 model**: claude-fable-5（大半），synthetic（システム内部処理）
- **関与 tool**: Agent（サブエージェント派遣），SendMessage，Bash（`ps`/`free`/`kill`/`git`/`gh`/`lake`），ScheduleWakeup，TaskStop，Edit/Write（memory・CLAUDE.md 追記）
- **役割**: orchestrator（`uda-lab/leray-hopf` 側の低優先度 issue 群を継続オーケストレーションで PR merge まで完遂させる；自らは Lean を直接編集せず，複数の named agent（issue155/155b/156/154 等）を派遣・監督）

## Event 列

| code | timestamp (UTC) | 一行説明 | incident候補 |
|---|---|---|---|
| WT-CONFLICT | 2026-07-18T04:00 | 前任 agent `issue155` が長時間無応答（`TaskOutput` も "No task found"）と判定され，後継 `issue155b` を同一 worktree `wt-155` に再dispatch．実際には `issue155` は生存しており，両者が同一 worktree に対して並行稼働する状態が発生 | yes |
| RESOURCE | 2026-07-18T06:00–06:01 | `issue155b` の targeted build（`lake build LerayHopf.Torus.ConvectionExtension`）が `.lake` のハードリンク共有規約を知らずに mathlib をソースから再構築中，バックグラウンドプロセスが 1802/1851 ファイル地点で無言のまま停止（`killed`，コンパイルエラーではない）．原因不明のまま detached（setsid+nohup+disown）で再起動 | yes |
| HANDOFF-FAIL | 2026-07-18T06:28–06:31 | オーケストレータがビルド進捗確認時に「lean プロセス合計 ~850MB でヘッドルーム十分，OOM の危険はいまのところない」と誤診断．実際には未回収の codex broker/app-server ゾンビ 14 系統が並行して蓄積しており，2 時間後に判明する resource 枯渇の予兆を見落としていた | yes |
| RESOURCE | 2026-07-18T07:40 | owner の "status" 一言を契機にオーケストレータが `free -h` を実行し，`free` 196Mi・`available` 606Mi（used 3.2Gi/total 3.8Gi．cgroup 実効上限は `memory.max`=3.42GiB）まで枯渇していることを初めて検出．owner からの検出ではなく，owner の一言が契機になった点が特徴 | yes |
| RESOURCE | 2026-07-18T07:41–07:42 | `ps aux`＋`readlink /proc/<pid>/cwd` で codex `app-server-broker.mjs`／`codex app-server` の常駐チェーンを追跡し，merge済み・削除済み worktree に紐づくゾンビ 14 系統（計 28 プロセス，~1〜1.5GB）を特定 | yes |
| RECOVERY | 2026-07-18T07:42 | ゾンビ 14 系統を一括 kill．`used` 3.2Gi→1.6Gi，`available` 606Mi→2.2Gi に回復（現存の 1 系統のみ意図的に残置） | no |
| RESOURCE | 2026-07-18T07:44 | `cat /sys/fs/cgroup/memory.events` で `oom_kill=7` を確認．これまでの build 死（exit 137）はすべて OOM の SIGKILL であったと事後確定．直近の試行は mathlib 全 ~1930 ファイルを完走した後，target module 本体（`Torus/ConvectionExtension.lean`，RSS 1.8GB 超）のコンパイル中に殺されていたと判明 | yes |
| RECOVERY | 2026-07-18T07:46 | ヘッドルーム最大（available 2.5Gi）の状態で `flock /tmp/lean-build.lock lake build LerayHopf.Torus.ConvectionExtension` を再実行．今回は OOM されず 16:49（JST）に成功 | no |
| WT-CONFLICT | 2026-07-18T07:46 | 生存が確認された `issue155` の所有権を再確定し，後継 `issue155b` を `TaskStop` で停止．`issue155` は独自に `.lake` ハードリンク共有を構築し triage 39 件の編集を完了済みであったことが判明，二重体制を解消 | no |
| RECOVERY | 2026-07-18T07:47–07:51 | owner から「同種の放置 OOM は数回目」と指摘（お叱り）を受け，4 層で恒常対策を記録：①global memory 新規 `feedback_reap_agent_side_processes.md`（2026-05-09 tmux pane 事案との再犯関係を明記），②project memory `lean-pde-notes` 側 `feedback-container-memory-hygiene.md`，③`lean-pde` 側 `project_build_env_memory_ceiling.md` への追記，④`~/.claude/CLAUDE.md` へのコンテナ限定恒常規約の直接注入（recall 依存脱却が目的） | no |
| RESOURCE | 2026-07-18T12:42, 13:06 | 恒常対策導入後，`issue154`（#154 巨大 proof module 分割）の downstream 検証ビルド（`AubinLionsLimitPassage`，1539行，依存最多）で `available` が 2 度 1.9GiB まで低下．ただし `issue154` は新設の閾値報告プロトコル（available < 2GiB で報告）に従って自発的に報告し，オーケストレータが sweep でゾンビ ゼロ・`oom_kill` カウンタ不変（7 のまま）を確認．正当な重量級コンパイル負荷であり OOM は発生せず，恒常対策導入後に類似の低メモリ局面が「ゾンビ蓄積」ではなく「正当な build RSS」という別形態で再発したが，新プロトコルにより実害なく検知・確認された | yes |
| RECOVERY | 2026-07-18T13:10 | `issue154` が PR #176 merge 後，worktree 削除・自身の codex broker/app-server チェーン kill・branch/remote-ref 整理・`task completed` 化までを新恒常規約どおりに完遂（「模範的な店じまい」とオーケストレータが評価）．日内で新規 OOM は再発せず（`oom_kill` は終始 7 のまま） | no |

## 成果

- **触れた主要 Lean ファイル**: `LerayHopf/Torus/ConvectionExtension.lean`，`LerayHopf/R3/ConvectionExtension.lean`，`LerayHopf/R3/SpatialCompactness.lean`（#154 wave1），downstream importer 群（`Core.lean`，`RellichBall.lean`，`SpacetimePrecompact.lean`，`SolutionInterfaces.lean`，`SteklovAverages.lean`，`ArzelaAscoliTime.lean`，`EnergyWeakLsc.lean`，`ConvectionForm.lean`，`AubinLionsLimitPassage.lean` 等）
- **マージ済み PR（`leray-hopf`）**: #174（issue #156，contributor向けdocstring/private境界整理，owner 3巡レビュー対応後），#175（issue #155，Torus/R3 ConvectionExtension のdocstring/private境界triage），#176（issue #154 wave1，`SpatialCompactness.lean` 分割）
- **恒常規約の追加先**: `~/.claude/CLAUDE.md`「本コンテナ限定 — メモリ・プロセス衛生」節（新設），global memory `feedback_reap_agent_side_processes.md`（新設），project memory `feedback-container-memory-hygiene.md`（`lean-pde-notes`，新設）・`project_build_env_memory_ceiling.md`（`lean-pde`，追記）
- **未完了**: issue #154 の wave2–6（物理分割の残り）は issue にチェックリスト化のみで着手前

## coder・confidence

- **coder**: incident-analyst（team-lead からの委任タスクとして符号化）
- **confidence**: 中〜高．2026-07-18 区間（L6855〜L8065）はキーワード grep（`oom`/`exit 137`/`memory.events`/`app-server-broker`/`flock`/`free -h` 等）による全域走査＋該当箇所の個別文脈確認を実施．152 件のサブエージェント transcript（EV-0088〜EV-0239 の範囲）はメイン transcript 内の agent 報告（要約）経由でのみ参照しており，個別ファイルは未読．cgroup `memory.events`／`ps aux`／`free -h` の生出力は複数時点で直接確認しておりOOM発生の一次証拠として強い．dmesg 出力自体（個々のkillのタイムスタンプ）は権限上取得できず，`oom_kill` カウンタは累積値（コンテナ起動以降の累積とみられ，本セッション開始前の分を含む可能性は排除できない）である点に留意。

**Evidence**: EV-0247（メイン transcript，本記録の一次情報源．collected mtime 2026-07-18T15:49:29Z）。EV-0088〜EV-0239（同セッションのサブエージェント transcript／meta，152 件の Agent 派遣に対応する範囲；未個別精読）。
